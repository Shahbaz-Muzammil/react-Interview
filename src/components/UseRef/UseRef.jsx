import React from 'react';

const UseRef = () => {
    return (
        <div>
        <h1>hello UseRef</h1>
        <input type="text"/>
        <button>focus the input host </button>
        </div>
    );
};

export default UseRef;